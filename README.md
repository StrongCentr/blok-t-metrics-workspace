# Пирамида метрик — прототип рабочего места

Статический прототип для лидера Блок Т. Открывается как одна страница `index.html`.

## Локально

Откройте `index.html` в браузере или поднимите простой HTTP-сервер.

## GitHub Pages

Сайт публикуется из ветки `main`, папка `/` (root).
